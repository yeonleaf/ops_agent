# 비동기 티켓 시스템 필수 패키지
fastapi>=0.104.0
uvicorn>=0.24.0
streamlit>=1.28.0
requests>=2.31.0
pydantic>=2.4.0

# 기존 시스템 의존성 (이미 설치되어 있을 가능성 높음)
python-dotenv>=1.0.0
openai>=1.3.0
sqlite3  # Python 표준 라이브러리

# 선택적 의존성
# logging은 Python 표준 라이브러리
# json은 Python 표준 라이브러리
# datetime는 Python 표준 라이브러리
# typing은 Python 표준 라이브러리
# threading은 Python 표준 라이브러리
# uuid는 Python 표준 라이브러리